# SPDX-FileCopyrightText: 2026 Damián Búho <damian.buho@proton.me>
#
# SPDX-License-Identifier: MIT
#
# Generated by pf-ci from org.projectfile.ci — DO NOT EDIT.
# Regenerate with: pf-ci generate --target gha
# A freshness gate (pf-ci generate --check) runs in CI so this file cannot
# silently drift from the source DAG.
---
name: published
"on":
  push: {}
  pull_request: {}
  workflow_dispatch:
    inputs:
      B19_COLOR:
        type: string
        default: "1"
      B19_FETCH_DOCKER_CACHE:
        type: string
        default: "Y"
      B19_FETCH_LOCAL_CACHE:
        type: string
        default: "Y"
      B19_OFFGRID_MODE:
        type: string
        default: "N"
      B19_UBUNTU_SERIES:
        type: string
        default: "resolute"
      B19_VERBOSITY:
        type: string
        default: "info"
      DESTINATION_DOCKER_REGISTRY:
        type: string
      GOPROXY:
        type: string
      LANG:
        type: string
      M6E_APT_CACHE_HOST:
        type: string
      M6E_APT_CACHE_PORT:
        type: string
      M6E_BUILD_DEBUG:
        type: string
      M6E_NEAR_CACHE_HOST:
        type: string
      M6E_NEAR_CACHE_PORT:
        type: string
      SOURCE_DOCKER_REGISTRY:
        type: string
permissions:
  contents: read
env:
  M6E_TAG: ${{ vars.BASE_IMAGE_DEFAULT_VERSION || 'latest' }}
  M6E_RUN_SCOPE: ${{ github.run_id }}-${{ github.run_attempt }}
  GOPROXY: https://kiota.ch/api/packages/projectfile/go,direct
jobs:
  artifacts-are-ok:
    runs-on: ubuntu-latest
    needs: ["binaries-are-ok", "image-is-ready", "inputs-are-ok"]
    steps:
      - run: echo artifacts-are-ok
  binaries-are-built:
    runs-on: ubuntu-latest
    needs: ["inputs-are-ok"]
    strategy:
      matrix:
        GOARCH: ["amd64", "arm64", "riscv64"]
        GOOS: ["linux", "darwin"]
        exclude:
          - GOARCH: "riscv64"
            GOOS: "darwin"
    env:
      GOARCH: ${{ matrix.GOARCH }}
      GOOS: ${{ matrix.GOOS }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: true
          persist-credentials: false
      - name: restore go
        uses: actions/cache/restore@caa296126883cff596d87d8935842f9db880ef25 # v5
        if: (vars.CI_ONLY_GOARCH == '' || contains(format(',{0},', vars.CI_ONLY_GOARCH), format(',{0},', matrix.GOARCH))) && (vars.CI_ONLY_GOOS == '' || contains(format(',{0},', vars.CI_ONLY_GOOS), format(',{0},', matrix.GOOS))) && vars.CI_SKIP_BINARIES_ARE_BUILT != 'true' && vars.CI_SKIP_BUILD_BINARIES != 'true'
        with:
          path: ${{ runner.temp }}/ci-cache/go
          key: ci-cache-go-
          restore-keys: |
            ci-cache-go-
      - name: build-binaries
        if: (vars.CI_ONLY_GOARCH == '' || contains(format(',{0},', vars.CI_ONLY_GOARCH), format(',{0},', matrix.GOARCH))) && (vars.CI_ONLY_GOOS == '' || contains(format(',{0},', vars.CI_ONLY_GOOS), format(',{0},', matrix.GOOS))) && vars.CI_SKIP_BINARIES_ARE_BUILT != 'true' && vars.CI_SKIP_BUILD_BINARIES != 'true'
        uses: projectfile-org/actions/run-tool@78f06c9316d295ea7b646ad1bdac87b76b7f999a # v1
        with:
          image: ${{ vars.GO_TOOL_IMAGE || format('{0}/b19/go', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: .scripts/build-binaries.sh
          env: |-
            GOARCH=${{ matrix.GOARCH }}
            GOOS=${{ matrix.GOOS }}
            GITHUB_REF_NAME
            GOPROXY
          mounts: ${{ runner.temp }}/ci-cache/go:/app/.cache/go:rw
      - name: save go
        uses: actions/cache/save@caa296126883cff596d87d8935842f9db880ef25 # v5
        if: (vars.CI_ONLY_GOARCH == '' || contains(format(',{0},', vars.CI_ONLY_GOARCH), format(',{0},', matrix.GOARCH))) && (vars.CI_ONLY_GOOS == '' || contains(format(',{0},', vars.CI_ONLY_GOOS), format(',{0},', matrix.GOOS))) && vars.CI_SKIP_BINARIES_ARE_BUILT != 'true' && vars.CI_SKIP_BUILD_BINARIES != 'true'
        with:
          path: ${{ runner.temp }}/ci-cache/go
          key: ci-cache-go-${{ github.run_id }}
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        if: (vars.CI_ONLY_GOARCH == '' || contains(format(',{0},', vars.CI_ONLY_GOARCH), format(',{0},', matrix.GOARCH))) && (vars.CI_ONLY_GOOS == '' || contains(format(',{0},', vars.CI_ONLY_GOOS), format(',{0},', matrix.GOOS))) && vars.CI_SKIP_BINARIES_ARE_BUILT != 'true' && vars.CI_SKIP_BUILD_BINARIES != 'true'
        with:
          name: build-binaries-${{ matrix.GOARCH }}-${{ matrix.GOOS }}-${{ github.run_id }}
          path: dist
          overwrite: true
  binaries-are-ok:
    runs-on: ubuntu-latest
    needs: ["binaries-are-built", "gsa"]
    steps:
      - run: echo binaries-are-ok
  binaries-are-released:
    runs-on: ubuntu-latest
    needs: ["binaries-are-torrented", "image-is-attested"]
    permissions:
      contents: write
    if: startsWith(github.ref, 'refs/tags/')
    strategy:
      matrix:
        GOARCH: ["amd64", "arm64", "riscv64"]
        GOOS: ["linux", "darwin"]
        exclude:
          - GOARCH: "riscv64"
            GOOS: "darwin"
    env:
      GOARCH: ${{ matrix.GOARCH }}
      GOOS: ${{ matrix.GOOS }}
      RELEASE_TAG: ${{ github.ref_name }}
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: true
          persist-credentials: false
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        if: (vars.CI_ONLY_GOARCH == '' || contains(format(',{0},', vars.CI_ONLY_GOARCH), format(',{0},', matrix.GOARCH))) && (vars.CI_ONLY_GOOS == '' || contains(format(',{0},', vars.CI_ONLY_GOOS), format(',{0},', matrix.GOOS)))
        with:
          name: torrent-create-${{ github.run_id }}
          path: dist
      - name: gh-release
        if: (vars.CI_ONLY_GOARCH == '' || contains(format(',{0},', vars.CI_ONLY_GOARCH), format(',{0},', matrix.GOARCH))) && (vars.CI_ONLY_GOOS == '' || contains(format(',{0},', vars.CI_ONLY_GOOS), format(',{0},', matrix.GOOS))) && vars.CI_SKIP_BINARIES_ARE_RELEASED != 'true' && vars.CI_SKIP_GH_RELEASE != 'true'
        run: .scripts/gh-release.sh "$RELEASE_TAG"
  binaries-are-signed:
    runs-on: ubuntu-latest
    needs: ["binaries-are-ok"]
    if: startsWith(github.ref, 'refs/tags/')
    strategy:
      matrix:
        GOARCH: ["amd64", "arm64", "riscv64"]
        GOOS: ["linux", "darwin"]
        exclude:
          - GOARCH: "riscv64"
            GOOS: "darwin"
    env:
      GOARCH: ${{ matrix.GOARCH }}
      GOOS: ${{ matrix.GOOS }}
      M6E_GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}
      M6E_GPG_PRIVATE_KEY: ${{ secrets.GPG_PRIVATE_KEY }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: true
          persist-credentials: false
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        if: (vars.CI_ONLY_GOARCH == '' || contains(format(',{0},', vars.CI_ONLY_GOARCH), format(',{0},', matrix.GOARCH))) && (vars.CI_ONLY_GOOS == '' || contains(format(',{0},', vars.CI_ONLY_GOOS), format(',{0},', matrix.GOOS)))
        with:
          name: build-binaries-${{ matrix.GOARCH }}-${{ matrix.GOOS }}-${{ github.run_id }}
          path: dist
      - name: gpg-key-file
        if: (vars.CI_ONLY_GOARCH == '' || contains(format(',{0},', vars.CI_ONLY_GOARCH), format(',{0},', matrix.GOARCH))) && (vars.CI_ONLY_GOOS == '' || contains(format(',{0},', vars.CI_ONLY_GOOS), format(',{0},', matrix.GOOS))) && vars.CI_SKIP_BINARIES_ARE_SIGNED != 'true' && vars.CI_SKIP_GPG_KEY_FILE != 'true'
        run: .makefile/core/scripts/gpg-key-file.sh
      - name: gpg-sign
        if: (vars.CI_ONLY_GOARCH == '' || contains(format(',{0},', vars.CI_ONLY_GOARCH), format(',{0},', matrix.GOARCH))) && (vars.CI_ONLY_GOOS == '' || contains(format(',{0},', vars.CI_ONLY_GOOS), format(',{0},', matrix.GOOS))) && vars.CI_SKIP_BINARIES_ARE_SIGNED != 'true' && vars.CI_SKIP_GPG_SIGN != 'true'
        uses: projectfile-org/actions/run-tool@78f06c9316d295ea7b646ad1bdac87b76b7f999a # v1
        with:
          image: ${{ vars.D9T_MISC_TOOLS_IMAGE || format('{0}/d9t/misc-tools', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: .makefile/core/scripts/gpg-sign.sh --cell dist/pf-cli
          env: |-
            GOARCH=${{ matrix.GOARCH }}
            GOOS=${{ matrix.GOOS }}
            M6E_GPG_PASSPHRASE=${{ secrets.GPG_PASSPHRASE }}
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        if: (vars.CI_ONLY_GOARCH == '' || contains(format(',{0},', vars.CI_ONLY_GOARCH), format(',{0},', matrix.GOARCH))) && (vars.CI_ONLY_GOOS == '' || contains(format(',{0},', vars.CI_ONLY_GOOS), format(',{0},', matrix.GOOS))) && vars.CI_SKIP_BINARIES_ARE_SIGNED != 'true' && vars.CI_SKIP_GPG_SIGN != 'true'
        with:
          name: gpg-sign-${{ matrix.GOARCH }}-${{ matrix.GOOS }}-${{ github.run_id }}
          path: dist
          overwrite: true
  binaries-are-torrented:
    runs-on: ubuntu-latest
    needs: ["binaries-are-signed"]
    if: startsWith(github.ref, 'refs/tags/')
    env:
      M6E_TORRENT_OUT_DIR: dist/torrents
      M6E_TORRENT_TRACKERS: https://tracker.kiota.ch/announce udp://tracker.kiota.ch:6969 wss://tracker.kiota.ch
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: true
          persist-credentials: false
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: gpg-sign-amd64-linux-${{ github.run_id }}
          path: dist
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: gpg-sign-amd64-darwin-${{ github.run_id }}
          path: dist
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: gpg-sign-arm64-linux-${{ github.run_id }}
          path: dist
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: gpg-sign-arm64-darwin-${{ github.run_id }}
          path: dist
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: gpg-sign-riscv64-linux-${{ github.run_id }}
          path: dist
      - name: torrent-create
        if: vars.CI_SKIP_BINARIES_ARE_TORRENTED != 'true' && vars.CI_SKIP_TORRENT_CREATE != 'true'
        uses: projectfile-org/actions/run-tool@78f06c9316d295ea7b646ad1bdac87b76b7f999a # v1
        with:
          image: ${{ vars.R8E_RQBIT_IMAGE || format('{0}/r8e/rqbit', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: .makefile/core/scripts/torrent.sh create --bundle dist/pf-cli
          env: |-
            M6E_TORRENT_OUT_DIR=dist/torrents
            M6E_TORRENT_TRACKERS=https://tracker.kiota.ch/announce udp://tracker.kiota.ch:6969 wss://tracker.kiota.ch
            GITHUB_REF_NAME
            GITHUB_REPOSITORY
            GITHUB_SERVER_URL
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        if: vars.CI_SKIP_BINARIES_ARE_TORRENTED != 'true' && vars.CI_SKIP_TORRENT_CREATE != 'true'
        with:
          name: torrent-create-${{ github.run_id }}
          path: dist
          overwrite: true
  container-is-ok:
    runs-on: ubuntu-latest
    needs: ["images-are-ok"]
    steps:
      - run: echo container-is-ok
  container-is-verified:
    runs-on: ubuntu-latest
    needs: ["container-ready-to-test"]
    env:
      M6E_IMAGE_FULLNAME: projectfile/cli:${{ vars.BASE_IMAGE_DEFAULT_VERSION || 'latest' }}-${{ github.run_id }}
      M6E_COMPOSE_PROJECT_NAME: ci-projectfile-cli-${{ github.run_id }}-${{ github.run_attempt }}
      M6E_CONTAINER_INSTANCE: ci-projectfile-cli-${{ github.run_id }}-${{ github.run_attempt }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: true
          persist-credentials: false
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: image-${{ github.run_id }}
      # The archive enters the runner's SHARED image store, where a sibling job's reap
      # can delete a layer record mid-copy — podman then buries the real cause under a
      # four-transport probe that claims the tar matches no supported format. The retry
      # for that class lives in the pinned action, not inline (Law 2).
      - uses: projectfile-org/actions/container-load@78f06c9316d295ea7b646ad1bdac87b76b7f999a # v1
        with:
          archive: image-${{ github.run_id }}.tar
      - name: dc-pull
        if: vars.CI_SKIP_IMAGES_ARE_OK != 'true' && vars.CI_SKIP_DC_PULL != 'true'
        run: M6E_COMPOSE_FILE=.compose/pipeline.yaml .makefile/container/scripts/compose-pull.sh
      - name: dc-up-d
        if: vars.CI_SKIP_CONTAINER_IS_OK != 'true' && vars.CI_SKIP_DC_UP_D != 'true'
        run: timeout --kill-after=10s "${M6E_COMPOSE_UP_TIMEOUT:-300}s" docker compose --project-name "$M6E_COMPOSE_PROJECT_NAME" --file .compose/pipeline.yaml up --detach --renew-anon-volumes --no-build </dev/null
      - name: container-test
        if: vars.CI_SKIP_CONTAINER_READY_TO_TEST != 'true' && vars.CI_SKIP_CONTAINER_TEST != 'true'
        uses: projectfile-org/actions/container-exec@78f06c9316d295ea7b646ad1bdac87b76b7f999a # v1
        with:
          container: ${{ env.M6E_CONTAINER_INSTANCE }}
          run: test.d
      - name: dc-down
        if: ${{ always() }}
        run: docker compose --project-name "$M6E_COMPOSE_PROJECT_NAME" --file .compose/pipeline.yaml down --volumes </dev/null
      # Reap the run-scoped image tag AND the live stack's network. Both are unique per
      # run (the tag carries M6E_RUN_SCOPE, the network the run-scoped project name), so
      # without this the runner's docker store gains one image per run forever AND a
      # failed/timed-out `up` can orphan the network (compose reports the removal error
      # WITHOUT failing). RmiImage and RmNetwork are always PAIRED (single assignment
      # site), so one docker-cleanup step reaps both — the imperative reaps + their
      # `inspect` guard live in the pinned action, not inline (Law 2).
      - if: ${{ always() }}
        uses: projectfile-org/actions/docker-cleanup@78f06c9316d295ea7b646ad1bdac87b76b7f999a # v1
        with:
          image: projectfile/cli:${{ env.M6E_TAG }}-${{ github.run_id }}
          network: ci-projectfile-cli-${{ env.M6E_RUN_SCOPE }}-network
  container-ready-to-test:
    runs-on: ubuntu-latest
    needs: ["container-is-ok"]
    steps:
      - run: echo container-ready-to-test
  documentation-is-ok:
    runs-on: ubuntu-latest
    needs: ["documentation-passes-lint"]
    steps:
      - run: echo documentation-is-ok
  documentation-passes-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: true
          persist-credentials: false
      - name: auto-markdownlint
        if: vars.CI_SKIP_DOCUMENTATION_PASSES_LINT != 'true' && vars.CI_SKIP_AUTO_MARKDOWNLINT != 'true'
        uses: projectfile-org/actions/run-tool@78f06c9316d295ea7b646ad1bdac87b76b7f999a # v1
        with:
          image: ${{ vars.D9T_JS_TOOLS_IMAGE || format('{0}/d9t/js-tools', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: auto-markdownlint
      - name: auto-textlint
        if: vars.CI_SKIP_DOCUMENTATION_PASSES_LINT != 'true' && vars.CI_SKIP_AUTO_TEXTLINT != 'true'
        uses: projectfile-org/actions/run-tool@78f06c9316d295ea7b646ad1bdac87b76b7f999a # v1
        with:
          image: ${{ vars.D9T_JS_TOOLS_IMAGE || format('{0}/d9t/js-tools', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: auto-textlint
  fetched:
    runs-on: ubuntu-latest
    steps:
      - run: echo fetched
  gsa:
    runs-on: ubuntu-latest
    needs: ["binaries-are-built"]
    strategy:
      matrix:
        GOARCH: ["amd64", "arm64", "riscv64"]
        GOOS: ["linux", "darwin"]
        exclude:
          - GOARCH: "riscv64"
            GOOS: "darwin"
    env:
      GOARCH: ${{ matrix.GOARCH }}
      GOOS: ${{ matrix.GOOS }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: true
          persist-credentials: false
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        if: (vars.CI_ONLY_GOARCH == '' || contains(format(',{0},', vars.CI_ONLY_GOARCH), format(',{0},', matrix.GOARCH))) && (vars.CI_ONLY_GOOS == '' || contains(format(',{0},', vars.CI_ONLY_GOOS), format(',{0},', matrix.GOOS)))
        with:
          name: build-binaries-${{ matrix.GOARCH }}-${{ matrix.GOOS }}-${{ github.run_id }}
          path: dist
      - name: auto-gsa
        if: (vars.CI_ONLY_GOARCH == '' || contains(format(',{0},', vars.CI_ONLY_GOARCH), format(',{0},', matrix.GOARCH))) && (vars.CI_ONLY_GOOS == '' || contains(format(',{0},', vars.CI_ONLY_GOOS), format(',{0},', matrix.GOOS))) && vars.CI_SKIP_GSA != 'true' && vars.CI_SKIP_AUTO_GSA != 'true'
        uses: projectfile-org/actions/run-tool@78f06c9316d295ea7b646ad1bdac87b76b7f999a # v1
        with:
          image: ${{ vars.D9T_GO_TOOLS_IMAGE || format('{0}/d9t/go-tools', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: .makefile/library/scripts/gsa-cell.sh dist/pf-cli
          env: |-
            GOARCH=${{ matrix.GOARCH }}
            GOOS=${{ matrix.GOOS }}
  image-has-sbom:
    runs-on: ubuntu-latest
    needs: ["image-is-built"]
    if: startsWith(github.ref, 'refs/tags/')
    env:
      M6E_IMAGE_ARCHIVE: image-${{ github.run_id }}.tar
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: true
          persist-credentials: false
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: image-${{ github.run_id }}
      - name: syft-sbom-tar
        if: vars.CI_SKIP_IMAGE_HAS_SBOM != 'true' && vars.CI_SKIP_SYFT_SBOM_TAR != 'true'
        uses: projectfile-org/actions/run-tool@78f06c9316d295ea7b646ad1bdac87b76b7f999a # v1
        with:
          image: ${{ vars.D9T_GO_TOOLS_IMAGE || format('{0}/d9t/go-tools', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: auto-syft image
          env: |-
            M6E_IMAGE_ARCHIVE=image-${{ github.run_id }}.tar
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        if: vars.CI_SKIP_IMAGE_HAS_SBOM != 'true' && vars.CI_SKIP_SYFT_SBOM_TAR != 'true'
        with:
          name: syft-sbom-tar-${{ github.run_id }}
          path: reports
          overwrite: true
  image-is-attested:
    runs-on: ubuntu-latest
    needs: ["image-has-sbom", "image-is-signed"]
    permissions:
      contents: read
      packages: write
    if: (startsWith(github.ref, 'refs/heads/') && github.ref != 'refs/heads/main') || (startsWith(github.ref, 'refs/tags/'))
    strategy:
      matrix:
        M6E_PUBLISH_SINK: ["ghcr"]
    env:
      COSIGN_PASSWORD: ${{ secrets.COSIGN_PASSWORD }}
      COSIGN_PRIVATE_KEY: ${{ secrets.COSIGN_PRIVATE_KEY }}
      DOCKERHUB_REGISTRY_PASSWORD: ${{ secrets.DOCKERHUB_REGISTRY_PASSWORD }}
      DOCKERHUB_REGISTRY_USERNAME: ${{ secrets.DOCKERHUB_REGISTRY_USERNAME }}
      GHCR_REGISTRY_PASSWORD: ${{ secrets.GITHUB_TOKEN }}
      GHCR_REGISTRY_USERNAME: ${{ github.actor }}
      REGISTRY_PASSWORD: ${{ secrets.REGISTRY_PASSWORD }}
      REGISTRY_USERNAME: ${{ secrets.REGISTRY_USERNAME }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: true
          persist-credentials: false
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        if: (vars.CI_ONLY_M6E_PUBLISH_SINK == '' || contains(format(',{0},', vars.CI_ONLY_M6E_PUBLISH_SINK), format(',{0},', matrix.M6E_PUBLISH_SINK)))
        with:
          name: build-binaries-amd64-linux-${{ github.run_id }}
          path: dist
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        if: (vars.CI_ONLY_M6E_PUBLISH_SINK == '' || contains(format(',{0},', vars.CI_ONLY_M6E_PUBLISH_SINK), format(',{0},', matrix.M6E_PUBLISH_SINK)))
        with:
          name: build-binaries-amd64-darwin-${{ github.run_id }}
          path: dist
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        if: (vars.CI_ONLY_M6E_PUBLISH_SINK == '' || contains(format(',{0},', vars.CI_ONLY_M6E_PUBLISH_SINK), format(',{0},', matrix.M6E_PUBLISH_SINK)))
        with:
          name: build-binaries-arm64-linux-${{ github.run_id }}
          path: dist
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        if: (vars.CI_ONLY_M6E_PUBLISH_SINK == '' || contains(format(',{0},', vars.CI_ONLY_M6E_PUBLISH_SINK), format(',{0},', matrix.M6E_PUBLISH_SINK)))
        with:
          name: build-binaries-arm64-darwin-${{ github.run_id }}
          path: dist
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        if: (vars.CI_ONLY_M6E_PUBLISH_SINK == '' || contains(format(',{0},', vars.CI_ONLY_M6E_PUBLISH_SINK), format(',{0},', matrix.M6E_PUBLISH_SINK)))
        with:
          name: build-binaries-riscv64-linux-${{ github.run_id }}
          path: dist
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        if: (vars.CI_ONLY_M6E_PUBLISH_SINK == '' || contains(format(',{0},', vars.CI_ONLY_M6E_PUBLISH_SINK), format(',{0},', matrix.M6E_PUBLISH_SINK)))
        with:
          name: image-${{ github.run_id }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        if: (vars.CI_ONLY_M6E_PUBLISH_SINK == '' || contains(format(',{0},', vars.CI_ONLY_M6E_PUBLISH_SINK), format(',{0},', matrix.M6E_PUBLISH_SINK)))
        with:
          name: syft-sbom-tar-${{ github.run_id }}
          path: reports
      - name: oci-push
        # The cell's run-time override gate, on the STEP because a job `if:` may not read the matrix context
        if: (vars.CI_ONLY_M6E_PUBLISH_SINK == '' || contains(format(',{0},', vars.CI_ONLY_M6E_PUBLISH_SINK), format(',{0},', matrix.M6E_PUBLISH_SINK))) && vars.CI_SKIP_PUBLISH_IMAGE != 'true' && vars.CI_SKIP_OCI_PUSH != 'true'
        uses: projectfile-org/actions/oci-push@78f06c9316d295ea7b646ad1bdac87b76b7f999a # v1
        with:
          artifact-name: image-${{ github.run_id }}
          image: projectfile/cli:${{ env.M6E_TAG }}-${{ github.run_id }}
          version: ${{ github.ref_name }}
          # The branch this run is on, empty on a tag. Non-empty collapses the cascade to
          # one `latest-<branch>` tag on the primary sink, so a branch build is testable
          # without a tag and without moving anything a consumer floats on.
          preview: ${{ github.ref_type == 'branch' && github.ref_name || '' }}
          # OUTPUT_REGISTRY: the forge-dependent PUSH target (kiota.ch / ghcr.io / …),
          # always threaded — empty => Docker Hub. NOT the per-namespace INPUT registries
          # (B19_DOCKER_REGISTRY & friends answer "where base images come FROM"); this
          # answers "where THIS project publishes TO". Set per forge (org var = default,
          # repo var = project-wise override). Ignored when `refs` below is present.
          registry: ${{ vars.OUTPUT_REGISTRY }}
          # Every destination this forge's route declares, each reference composed by
          # the sink that declared it — so one archive lands under whatever path grammar
          # each registry wants. Credentials key on the SINK name.
          refs: |
            ghcr ghcr.io/projectfile-org/cli:latest
          # This CELL's destination, off the publish axis: the job runs one cell per
          # place the archive goes, so a registry refusing a push fails only its own.
          sink: ${{ matrix.M6E_PUBLISH_SINK }}
      - name: cosign-key-file
        if: (vars.CI_ONLY_M6E_PUBLISH_SINK == '' || contains(format(',{0},', vars.CI_ONLY_M6E_PUBLISH_SINK), format(',{0},', matrix.M6E_PUBLISH_SINK))) && vars.CI_SKIP_IMAGE_IS_SIGNED != 'true' && vars.CI_SKIP_COSIGN_KEY_FILE != 'true'
        run: .makefile/b19/scripts/cosign-key-file.sh
      - name: cosign-sign
        if: (vars.CI_ONLY_M6E_PUBLISH_SINK == '' || contains(format(',{0},', vars.CI_ONLY_M6E_PUBLISH_SINK), format(',{0},', matrix.M6E_PUBLISH_SINK))) && vars.CI_SKIP_IMAGE_IS_SIGNED != 'true' && vars.CI_SKIP_COSIGN_SIGN != 'true'
        uses: projectfile-org/actions/run-tool@78f06c9316d295ea7b646ad1bdac87b76b7f999a # v1
        with:
          image: ${{ vars.D9T_GO_TOOLS_IMAGE || format('{0}/d9t/go-tools', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: auto-cosign sign
          env: |-
            COSIGN_PASSWORD=${{ secrets.COSIGN_PASSWORD }}
      - name: cosign-attest
        if: (vars.CI_ONLY_M6E_PUBLISH_SINK == '' || contains(format(',{0},', vars.CI_ONLY_M6E_PUBLISH_SINK), format(',{0},', matrix.M6E_PUBLISH_SINK))) && vars.CI_SKIP_IMAGE_IS_ATTESTED != 'true' && vars.CI_SKIP_COSIGN_ATTEST != 'true'
        uses: projectfile-org/actions/run-tool@78f06c9316d295ea7b646ad1bdac87b76b7f999a # v1
        with:
          image: ${{ vars.D9T_GO_TOOLS_IMAGE || format('{0}/d9t/go-tools', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: auto-cosign attest
          env: |-
            COSIGN_PASSWORD=${{ secrets.COSIGN_PASSWORD }}
  image-is-built:
    runs-on: ubuntu-latest
    needs: ["fetched", "inputs-are-ok", "schema-fetched"]
    permissions:
      actions: write
      contents: read
    concurrency:
      group: build-${{ github.repository }}-${{ toJSON(matrix) }}
      cancel-in-progress: false
    env:
      M6E_NAMESPACE: projectfile
      M6E_PROJECT: cli
      B19_COLOR: ${{ inputs.B19_COLOR || vars.B19_COLOR || '1' }}
      B19_FETCH_DOCKER_CACHE: ${{ inputs.B19_FETCH_DOCKER_CACHE || vars.B19_FETCH_DOCKER_CACHE || 'Y' }}
      B19_FETCH_LOCAL_CACHE: ${{ inputs.B19_FETCH_LOCAL_CACHE || vars.B19_FETCH_LOCAL_CACHE || 'Y' }}
      B19_GO_BASE_IMAGE: ${{ vars.B19_GO_BASE_IMAGE || format('{0}/b19/go:{1}', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho', vars.BASE_IMAGE_DEFAULT_VERSION || 'latest') }}
      B19_OFFGRID_MODE: ${{ inputs.B19_OFFGRID_MODE || vars.B19_OFFGRID_MODE || 'N' }}
      B19_UBUNTU_BASE_IMAGE: ${{ vars.B19_UBUNTU_BASE_IMAGE || format('{0}/b19/ubuntu/{1}:{2}', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho', vars.B19_UBUNTU_SERIES || 'resolute', vars.BASE_IMAGE_DEFAULT_VERSION || 'latest') }}
      B19_UBUNTU_SERIES: ${{ inputs.B19_UBUNTU_SERIES || vars.B19_UBUNTU_SERIES || 'resolute' }}
      B19_VERBOSITY: ${{ inputs.B19_VERBOSITY || vars.B19_VERBOSITY || 'info' }}
      DESTINATION_DOCKER_REGISTRY: ${{ inputs.DESTINATION_DOCKER_REGISTRY || vars.DESTINATION_DOCKER_REGISTRY }}
      GOPROXY: ${{ inputs.GOPROXY || vars.GOPROXY }}
      LANG: ${{ inputs.LANG || vars.LANG }}
      M6E_APT_CACHE_HOST: ${{ inputs.M6E_APT_CACHE_HOST || vars.M6E_APT_CACHE_HOST }}
      M6E_APT_CACHE_PORT: ${{ inputs.M6E_APT_CACHE_PORT || vars.M6E_APT_CACHE_PORT }}
      M6E_BUILD_DEBUG: ${{ inputs.M6E_BUILD_DEBUG || vars.M6E_BUILD_DEBUG }}
      M6E_NEAR_CACHE_HOST: ${{ inputs.M6E_NEAR_CACHE_HOST || vars.M6E_NEAR_CACHE_HOST }}
      M6E_NEAR_CACHE_PORT: ${{ inputs.M6E_NEAR_CACHE_PORT || vars.M6E_NEAR_CACHE_PORT }}
      SOURCE_DOCKER_REGISTRY: ${{ inputs.SOURCE_DOCKER_REGISTRY || vars.SOURCE_DOCKER_REGISTRY }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: true
          persist-credentials: false
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: fetch-schema-${{ github.run_id }}
          path: internal/validate/embedded
      - name: use an isolated buildx builder
        if: vars.CI_SKIP_IMAGE_IS_BUILT != 'true' && vars.CI_SKIP_CONTAINER_BUILD != 'true'
        # the docker driver's image exporter needs the host's containerd store; docker-container carries its own, so no host config or restart is needed on any GHA runner flavor.
        run: docker buildx create --name ci --driver docker-container --use
      - name: container-build
        if: vars.CI_SKIP_IMAGE_IS_BUILT != 'true' && vars.CI_SKIP_CONTAINER_BUILD != 'true'
        uses: projectfile-org/actions/container-build/buildx@78f06c9316d295ea7b646ad1bdac87b76b7f999a # v1
        with:
          artifact-name: image-${{ github.run_id }}
          image: projectfile/cli:${{ env.M6E_TAG }}-${{ github.run_id }}
          build-args: |-
            B19_COLOR=${{ inputs.B19_COLOR || vars.B19_COLOR || '1' }}
            B19_FETCH_DOCKER_CACHE=${{ inputs.B19_FETCH_DOCKER_CACHE || vars.B19_FETCH_DOCKER_CACHE || 'Y' }}
            B19_FETCH_LOCAL_CACHE=${{ inputs.B19_FETCH_LOCAL_CACHE || vars.B19_FETCH_LOCAL_CACHE || 'Y' }}
            B19_GO_BASE_IMAGE=${{ vars.B19_GO_BASE_IMAGE || format('{0}/b19/go:{1}', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho', env.M6E_TAG) }}
            B19_OFFGRID_MODE=${{ inputs.B19_OFFGRID_MODE || vars.B19_OFFGRID_MODE || 'N' }}
            B19_UBUNTU_BASE_IMAGE=${{ vars.B19_UBUNTU_BASE_IMAGE || format('{0}/b19/ubuntu/{1}:{2}', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho', vars.B19_UBUNTU_SERIES || 'resolute', env.M6E_TAG) }}
            B19_UBUNTU_SERIES=${{ inputs.B19_UBUNTU_SERIES || vars.B19_UBUNTU_SERIES || 'resolute' }}
            B19_VERBOSITY=${{ inputs.B19_VERBOSITY || vars.B19_VERBOSITY || 'info' }}
            DESTINATION_DOCKER_REGISTRY=${{ inputs.DESTINATION_DOCKER_REGISTRY || vars.DESTINATION_DOCKER_REGISTRY }}
            GOPROXY=${{ inputs.GOPROXY || vars.GOPROXY }}
            LANG=${{ inputs.LANG || vars.LANG }}
            M6E_APT_CACHE_HOST=${{ inputs.M6E_APT_CACHE_HOST || vars.M6E_APT_CACHE_HOST }}
            M6E_APT_CACHE_PORT=${{ inputs.M6E_APT_CACHE_PORT || vars.M6E_APT_CACHE_PORT }}
            M6E_BUILD_DEBUG=${{ inputs.M6E_BUILD_DEBUG || vars.M6E_BUILD_DEBUG }}
            M6E_NAMESPACE=projectfile
            M6E_NEAR_CACHE_HOST=${{ inputs.M6E_NEAR_CACHE_HOST || vars.M6E_NEAR_CACHE_HOST }}
            M6E_NEAR_CACHE_PORT=${{ inputs.M6E_NEAR_CACHE_PORT || vars.M6E_NEAR_CACHE_PORT }}
            M6E_PROJECT=cli
            SOURCE_DOCKER_REGISTRY=${{ inputs.SOURCE_DOCKER_REGISTRY || vars.SOURCE_DOCKER_REGISTRY }}
          mounts: fetch=/fetch
          pf-cli-image: ${{ vars.PF_CLI_IMAGE || format('{0}/projectfile/cli:{1}', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho', env.M6E_TAG) }}
          mount-cache-key: ci-cache-mounts-
          mount-cache-restore-keys: ci-cache-mounts-
  image-is-ready:
    runs-on: ubuntu-latest
    needs: ["container-is-verified", "image-is-secure"]
    steps:
      - run: echo image-is-ready
  image-is-secure:
    runs-on: ubuntu-latest
    needs: ["image-is-built"]
    env:
      M6E_IMAGE_ARCHIVE: image-${{ github.run_id }}.tar
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: true
          persist-credentials: false
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: image-${{ github.run_id }}
      - name: restore grype-db
        uses: actions/cache/restore@caa296126883cff596d87d8935842f9db880ef25 # v5
        if: vars.CI_SKIP_IMAGE_IS_SECURE != 'true' && vars.CI_SKIP_GRYPE_SCAN_TAR != 'true'
        with:
          path: ${{ runner.temp }}/ci-cache/grype-db
          key: ci-cache-grype-db-
          restore-keys: |
            ci-cache-grype-db-
      - name: grype-scan-tar
        if: vars.CI_SKIP_IMAGE_IS_SECURE != 'true' && vars.CI_SKIP_GRYPE_SCAN_TAR != 'true'
        uses: projectfile-org/actions/run-tool@78f06c9316d295ea7b646ad1bdac87b76b7f999a # v1
        with:
          image: ${{ vars.D9T_GO_TOOLS_IMAGE || format('{0}/d9t/go-tools', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: auto-grype image
          env: |-
            M6E_IMAGE_ARCHIVE=image-${{ github.run_id }}.tar
            M6E_SHARED_CACHE=N
          mounts: ${{ runner.temp }}/ci-cache/grype-db:/app/.cache/grype:rw
      - name: save grype-db
        uses: actions/cache/save@caa296126883cff596d87d8935842f9db880ef25 # v5
        if: vars.CI_SKIP_IMAGE_IS_SECURE != 'true' && vars.CI_SKIP_GRYPE_SCAN_TAR != 'true'
        with:
          path: ${{ runner.temp }}/ci-cache/grype-db
          key: ci-cache-grype-db-${{ github.run_id }}
      - name: restore trivy-db
        uses: actions/cache/restore@caa296126883cff596d87d8935842f9db880ef25 # v5
        if: vars.CI_SKIP_IMAGE_IS_SECURE != 'true' && vars.CI_SKIP_TRIVY_SCAN_TAR != 'true'
        with:
          path: ${{ runner.temp }}/ci-cache/trivy-db
          key: ci-cache-trivy-db-
          restore-keys: |
            ci-cache-trivy-db-
      - name: trivy-scan-tar
        if: vars.CI_SKIP_IMAGE_IS_SECURE != 'true' && vars.CI_SKIP_TRIVY_SCAN_TAR != 'true'
        uses: projectfile-org/actions/run-tool@78f06c9316d295ea7b646ad1bdac87b76b7f999a # v1
        with:
          image: ${{ vars.D9T_GO_TOOLS_IMAGE || format('{0}/d9t/go-tools', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: auto-trivy image
          env: |-
            M6E_IMAGE_ARCHIVE=image-${{ github.run_id }}.tar
            M6E_SHARED_CACHE=N
            TRIVY_CACHE_BACKEND
          mounts: ${{ runner.temp }}/ci-cache/trivy-db:/app/.cache/trivy:rw
      - name: save trivy-db
        uses: actions/cache/save@caa296126883cff596d87d8935842f9db880ef25 # v5
        if: vars.CI_SKIP_IMAGE_IS_SECURE != 'true' && vars.CI_SKIP_TRIVY_SCAN_TAR != 'true'
        with:
          path: ${{ runner.temp }}/ci-cache/trivy-db
          key: ci-cache-trivy-db-${{ github.run_id }}
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        if: ${{ always() }}
        with:
          name: reports-image-is-secure-${{ github.run_id }}
          path: reports/
          if-no-files-found: warn
          overwrite: true
  image-is-signed:
    runs-on: ubuntu-latest
    needs: ["publish-image"]
    if: startsWith(github.ref, 'refs/tags/')
    steps:
      - run: echo image-is-signed
  images-are-ok:
    runs-on: ubuntu-latest
    needs: ["image-is-built", "secrets-ready"]
    steps:
      - run: echo images-are-ok
  inputs-are-ok:
    runs-on: ubuntu-latest
    needs: ["documentation-is-ok", "projectfile-is-ok", "source-is-ok"]
    steps:
      - run: echo inputs-are-ok
  projectfile-is-ok:
    runs-on: ubuntu-latest
    needs: ["projectfile-is-valid"]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: true
          persist-credentials: false
      - name: pf-bridge-check
        if: vars.CI_SKIP_PROJECTFILE_IS_OK != 'true' && vars.CI_SKIP_PF_BRIDGE_CHECK != 'true'
        uses: projectfile-org/actions/run-tool@78f06c9316d295ea7b646ad1bdac87b76b7f999a # v1
        with:
          image: ${{ vars.PF_BRIDGE_IMAGE || format('{0}/projectfile/bridge', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: pf-bridge check
  projectfile-is-valid:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: true
          persist-credentials: false
      - name: pf-validate
        if: vars.CI_SKIP_PROJECTFILE_IS_VALID != 'true' && vars.CI_SKIP_PF_VALIDATE != 'true'
        uses: projectfile-org/actions/run-tool@78f06c9316d295ea7b646ad1bdac87b76b7f999a # v1
        with:
          image: ${{ vars.PF_CLI_IMAGE || format('{0}/projectfile/cli', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: pf-cli validate
  publish-image:
    runs-on: ubuntu-latest
    needs: ["image-is-built", "ready-to-publish", "signing-is-ready"]
    if: (startsWith(github.ref, 'refs/heads/') && github.ref != 'refs/heads/main') || (startsWith(github.ref, 'refs/tags/'))
    steps:
      - run: echo publish-image
  published:
    runs-on: ubuntu-latest
    needs: ["binaries-are-released", "image-is-attested", "publish-image", "ready-to-publish"]
    steps:
      - run: echo published
  ready-to-publish:
    runs-on: ubuntu-latest
    needs: ["artifacts-are-ok"]
    steps:
      - run: echo ready-to-publish
  schema-fetched:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: true
          persist-credentials: false
      - name: fetch-schema
        if: vars.CI_SKIP_SCHEMA_FETCHED != 'true' && vars.CI_SKIP_FETCH_SCHEMA != 'true'
        run: .scripts/fetch-schema.sh
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        if: vars.CI_SKIP_SCHEMA_FETCHED != 'true' && vars.CI_SKIP_FETCH_SCHEMA != 'true'
        with:
          name: fetch-schema-${{ github.run_id }}
          path: internal/validate/embedded
          overwrite: true
  secrets-ready:
    runs-on: ubuntu-latest
    needs: ["image-is-built"]
    steps:
      - run: echo secrets-ready
  signing-is-ready:
    runs-on: ubuntu-latest
    if: (startsWith(github.ref, 'refs/heads/') && github.ref != 'refs/heads/main') || (startsWith(github.ref, 'refs/tags/'))
    env:
      COSIGN_PASSWORD: ${{ secrets.COSIGN_PASSWORD }}
      COSIGN_PRIVATE_KEY: ${{ secrets.COSIGN_PRIVATE_KEY }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: true
          persist-credentials: false
      - name: cosign-key-file-preflight
        if: vars.CI_SKIP_SIGNING_IS_READY != 'true' && vars.CI_SKIP_COSIGN_KEY_FILE_PREFLIGHT != 'true'
        run: .makefile/b19/scripts/cosign-key-file.sh
      - name: cosign-preflight
        if: vars.CI_SKIP_SIGNING_IS_READY != 'true' && vars.CI_SKIP_COSIGN_PREFLIGHT != 'true'
        uses: projectfile-org/actions/run-tool@78f06c9316d295ea7b646ad1bdac87b76b7f999a # v1
        with:
          image: ${{ vars.D9T_GO_TOOLS_IMAGE || format('{0}/d9t/go-tools', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: auto-cosign preflight
          env: |-
            COSIGN_PASSWORD=${{ secrets.COSIGN_PASSWORD }}
  source-is-compliant:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: true
          persist-credentials: false
      - name: i18n-check
        if: vars.CI_SKIP_SOURCE_IS_COMPLIANT != 'true' && vars.CI_SKIP_I18N_CHECK != 'true'
        uses: projectfile-org/actions/run-tool@78f06c9316d295ea7b646ad1bdac87b76b7f999a # v1
        with:
          image: ${{ vars.D9T_MISC_TOOLS_IMAGE || format('{0}/d9t/misc-tools', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: auto-i18n check
      - name: reuse-lint
        if: vars.CI_SKIP_SOURCE_IS_COMPLIANT != 'true' && vars.CI_SKIP_REUSE_LINT != 'true'
        uses: projectfile-org/actions/run-tool@78f06c9316d295ea7b646ad1bdac87b76b7f999a # v1
        with:
          image: ${{ vars.D9T_PYTHON_TOOLS_IMAGE || format('{0}/d9t/python-tools', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: reuse lint
  source-is-ok:
    runs-on: ubuntu-latest
    needs: ["source-is-compliant", "source-is-secure", "source-is-tested", "source-passes-lint"]
    steps:
      - run: echo source-is-ok
  source-is-secure:
    runs-on: ubuntu-latest
    env:
      OSV_SCANNER_LOCAL_DB_CACHE_DIRECTORY: /app/.cache/osv
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: true
          persist-credentials: false
      - name: restore grype-db
        uses: actions/cache/restore@caa296126883cff596d87d8935842f9db880ef25 # v5
        if: vars.CI_SKIP_SOURCE_IS_SECURE != 'true' && vars.CI_SKIP_GRYPE_SCAN_SOURCE != 'true'
        with:
          path: ${{ runner.temp }}/ci-cache/grype-db
          key: ci-cache-grype-db-
          restore-keys: |
            ci-cache-grype-db-
      - name: grype-scan-source
        if: vars.CI_SKIP_SOURCE_IS_SECURE != 'true' && vars.CI_SKIP_GRYPE_SCAN_SOURCE != 'true'
        uses: projectfile-org/actions/run-tool@78f06c9316d295ea7b646ad1bdac87b76b7f999a # v1
        with:
          image: ${{ vars.D9T_GO_TOOLS_IMAGE || format('{0}/d9t/go-tools', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: auto-grype
          env: |-
            M6E_SHARED_CACHE=N
          mounts: ${{ runner.temp }}/ci-cache/grype-db:/app/.cache/grype:rw
      - name: save grype-db
        uses: actions/cache/save@caa296126883cff596d87d8935842f9db880ef25 # v5
        if: vars.CI_SKIP_SOURCE_IS_SECURE != 'true' && vars.CI_SKIP_GRYPE_SCAN_SOURCE != 'true'
        with:
          path: ${{ runner.temp }}/ci-cache/grype-db
          key: ci-cache-grype-db-${{ github.run_id }}
      - name: restore osv-db
        uses: actions/cache/restore@caa296126883cff596d87d8935842f9db880ef25 # v5
        if: vars.CI_SKIP_SOURCE_IS_SECURE != 'true' && vars.CI_SKIP_OSV_SCANNER_SCAN_SOURCE != 'true'
        with:
          path: ${{ runner.temp }}/ci-cache/osv-db
          key: ci-cache-osv-db-
          restore-keys: |
            ci-cache-osv-db-
      - name: osv-scanner-scan-source
        if: vars.CI_SKIP_SOURCE_IS_SECURE != 'true' && vars.CI_SKIP_OSV_SCANNER_SCAN_SOURCE != 'true'
        uses: projectfile-org/actions/run-tool@78f06c9316d295ea7b646ad1bdac87b76b7f999a # v1
        with:
          image: ${{ vars.D9T_GO_TOOLS_IMAGE || format('{0}/d9t/go-tools', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: auto-osv-scanner --offline-vulnerabilities
          env: |-
            OSV_SCANNER_LOCAL_DB_CACHE_DIRECTORY=/app/.cache/osv
            M6E_SHARED_CACHE=N
          mounts: ${{ runner.temp }}/ci-cache/osv-db:/app/.cache/osv:rw
      - name: save osv-db
        uses: actions/cache/save@caa296126883cff596d87d8935842f9db880ef25 # v5
        if: vars.CI_SKIP_SOURCE_IS_SECURE != 'true' && vars.CI_SKIP_OSV_SCANNER_SCAN_SOURCE != 'true'
        with:
          path: ${{ runner.temp }}/ci-cache/osv-db
          key: ci-cache-osv-db-${{ github.run_id }}
      - name: restore trivy-db
        uses: actions/cache/restore@caa296126883cff596d87d8935842f9db880ef25 # v5
        if: vars.CI_SKIP_SOURCE_IS_SECURE != 'true' && vars.CI_SKIP_TRIVY_SCAN_SOURCE != 'true'
        with:
          path: ${{ runner.temp }}/ci-cache/trivy-db
          key: ci-cache-trivy-db-
          restore-keys: |
            ci-cache-trivy-db-
      - name: trivy-scan-source
        if: vars.CI_SKIP_SOURCE_IS_SECURE != 'true' && vars.CI_SKIP_TRIVY_SCAN_SOURCE != 'true'
        uses: projectfile-org/actions/run-tool@78f06c9316d295ea7b646ad1bdac87b76b7f999a # v1
        with:
          image: ${{ vars.D9T_GO_TOOLS_IMAGE || format('{0}/d9t/go-tools', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: auto-trivy fs
          env: |-
            M6E_SHARED_CACHE=N
            TRIVY_CACHE_BACKEND
          mounts: ${{ runner.temp }}/ci-cache/trivy-db:/app/.cache/trivy:rw
      - name: save trivy-db
        uses: actions/cache/save@caa296126883cff596d87d8935842f9db880ef25 # v5
        if: vars.CI_SKIP_SOURCE_IS_SECURE != 'true' && vars.CI_SKIP_TRIVY_SCAN_SOURCE != 'true'
        with:
          path: ${{ runner.temp }}/ci-cache/trivy-db
          key: ci-cache-trivy-db-${{ github.run_id }}
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        if: ${{ always() }}
        with:
          name: reports-source-is-secure-${{ github.run_id }}
          path: reports/
          if-no-files-found: warn
          overwrite: true
  source-is-tested:
    runs-on: ubuntu-latest
    needs: ["schema-fetched"]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: true
          persist-credentials: false
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: fetch-schema-${{ github.run_id }}
          path: internal/validate/embedded
      - name: restore go
        uses: actions/cache/restore@caa296126883cff596d87d8935842f9db880ef25 # v5
        if: vars.CI_SKIP_SOURCE_IS_TESTED != 'true' && vars.CI_SKIP_GO_TEST != 'true'
        with:
          path: ${{ runner.temp }}/ci-cache/go
          key: ci-cache-go-
          restore-keys: |
            ci-cache-go-
      - name: go-test
        if: vars.CI_SKIP_SOURCE_IS_TESTED != 'true' && vars.CI_SKIP_GO_TEST != 'true'
        uses: projectfile-org/actions/run-tool@78f06c9316d295ea7b646ad1bdac87b76b7f999a # v1
        with:
          image: ${{ vars.GO_TOOL_IMAGE || format('{0}/b19/go', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: go test -coverprofile=coverage.out ./...
          env: |-
            GOPROXY
          mounts: ${{ runner.temp }}/ci-cache/go:/app/.cache/go:rw
      - name: save go
        uses: actions/cache/save@caa296126883cff596d87d8935842f9db880ef25 # v5
        if: vars.CI_SKIP_SOURCE_IS_TESTED != 'true' && vars.CI_SKIP_GO_TEST != 'true'
        with:
          path: ${{ runner.temp }}/ci-cache/go
          key: ci-cache-go-${{ github.run_id }}
  source-passes-lint:
    runs-on: ubuntu-latest
    needs: ["schema-fetched"]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: true
          persist-credentials: false
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: fetch-schema-${{ github.run_id }}
          path: internal/validate/embedded
      - name: auto-cffr-validate
        if: vars.CI_SKIP_SOURCE_PASSES_LINT != 'true' && vars.CI_SKIP_AUTO_CFFR_VALIDATE != 'true'
        uses: projectfile-org/actions/run-tool@78f06c9316d295ea7b646ad1bdac87b76b7f999a # v1
        with:
          image: ${{ vars.D9T_R_TOOLS_IMAGE || format('{0}/d9t/r-tools', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: auto-cffr-validate
      - name: restore go
        uses: actions/cache/restore@caa296126883cff596d87d8935842f9db880ef25 # v5
        if: vars.CI_SKIP_SOURCE_PASSES_LINT != 'true' && vars.CI_SKIP_AUTO_GOLANGCI_LINT != 'true'
        with:
          path: ${{ runner.temp }}/ci-cache/go
          key: ci-cache-go-
          restore-keys: |
            ci-cache-go-
      - name: auto-golangci-lint
        if: vars.CI_SKIP_SOURCE_PASSES_LINT != 'true' && vars.CI_SKIP_AUTO_GOLANGCI_LINT != 'true'
        uses: projectfile-org/actions/run-tool@78f06c9316d295ea7b646ad1bdac87b76b7f999a # v1
        with:
          image: ${{ vars.D9T_GO_TOOLS_IMAGE || format('{0}/d9t/go-tools', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: auto-golangci-lint
          env: |-
            GOPROXY
          mounts: ${{ runner.temp }}/ci-cache/go:/app/.cache/go:rw
      - name: save go
        uses: actions/cache/save@caa296126883cff596d87d8935842f9db880ef25 # v5
        if: vars.CI_SKIP_SOURCE_PASSES_LINT != 'true' && vars.CI_SKIP_AUTO_GOLANGCI_LINT != 'true'
        with:
          path: ${{ runner.temp }}/ci-cache/go
          key: ci-cache-go-${{ github.run_id }}
      - name: restore go
        uses: actions/cache/restore@caa296126883cff596d87d8935842f9db880ef25 # v5
        if: vars.CI_SKIP_SOURCE_PASSES_LINT != 'true' && vars.CI_SKIP_AUTO_GOSEC != 'true'
        with:
          path: ${{ runner.temp }}/ci-cache/go
          key: ci-cache-go-
          restore-keys: |
            ci-cache-go-
      - name: auto-gosec
        if: vars.CI_SKIP_SOURCE_PASSES_LINT != 'true' && vars.CI_SKIP_AUTO_GOSEC != 'true'
        uses: projectfile-org/actions/run-tool@78f06c9316d295ea7b646ad1bdac87b76b7f999a # v1
        with:
          image: ${{ vars.D9T_GO_TOOLS_IMAGE || format('{0}/d9t/go-tools', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: auto-gosec
          env: |-
            GOPROXY
          mounts: ${{ runner.temp }}/ci-cache/go:/app/.cache/go:rw
      - name: save go
        uses: actions/cache/save@caa296126883cff596d87d8935842f9db880ef25 # v5
        if: vars.CI_SKIP_SOURCE_PASSES_LINT != 'true' && vars.CI_SKIP_AUTO_GOSEC != 'true'
        with:
          path: ${{ runner.temp }}/ci-cache/go
          key: ci-cache-go-${{ github.run_id }}
      - name: auto-jsonlint
        if: vars.CI_SKIP_SOURCE_PASSES_LINT != 'true' && vars.CI_SKIP_AUTO_JSONLINT != 'true'
        uses: projectfile-org/actions/run-tool@78f06c9316d295ea7b646ad1bdac87b76b7f999a # v1
        with:
          image: ${{ vars.D9T_JS_TOOLS_IMAGE || format('{0}/d9t/js-tools', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: auto-jsonlint
      - name: auto-shellcheck
        if: vars.CI_SKIP_SOURCE_PASSES_LINT != 'true' && vars.CI_SKIP_AUTO_SHELLCHECK != 'true'
        uses: projectfile-org/actions/run-tool@78f06c9316d295ea7b646ad1bdac87b76b7f999a # v1
        with:
          image: ${{ vars.D9T_MISC_TOOLS_IMAGE || format('{0}/d9t/misc-tools', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: auto-shellcheck
      - name: auto-yamllint
        if: vars.CI_SKIP_SOURCE_PASSES_LINT != 'true' && vars.CI_SKIP_AUTO_YAMLLINT != 'true'
        uses: projectfile-org/actions/run-tool@78f06c9316d295ea7b646ad1bdac87b76b7f999a # v1
        with:
          image: ${{ vars.D9T_PYTHON_TOOLS_IMAGE || format('{0}/d9t/python-tools', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: auto-yamllint
      - name: auto-zizmor
        if: vars.CI_SKIP_SOURCE_PASSES_LINT != 'true' && vars.CI_SKIP_AUTO_ZIZMOR != 'true'
        uses: projectfile-org/actions/run-tool@78f06c9316d295ea7b646ad1bdac87b76b7f999a # v1
        with:
          image: ${{ vars.D9T_RUST_TOOLS_IMAGE || format('{0}/d9t/rust-tools', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: auto-zizmor
      - name: restore go
        uses: actions/cache/restore@caa296126883cff596d87d8935842f9db880ef25 # v5
        if: vars.CI_SKIP_SOURCE_PASSES_LINT != 'true' && vars.CI_SKIP_GO_VET != 'true'
        with:
          path: ${{ runner.temp }}/ci-cache/go
          key: ci-cache-go-
          restore-keys: |
            ci-cache-go-
      - name: go-vet
        if: vars.CI_SKIP_SOURCE_PASSES_LINT != 'true' && vars.CI_SKIP_GO_VET != 'true'
        uses: projectfile-org/actions/run-tool@78f06c9316d295ea7b646ad1bdac87b76b7f999a # v1
        with:
          image: ${{ vars.GO_TOOL_IMAGE || format('{0}/b19/go', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: go vet ./...
          env: |-
            GOPROXY
          mounts: ${{ runner.temp }}/ci-cache/go:/app/.cache/go:rw
      - name: save go
        uses: actions/cache/save@caa296126883cff596d87d8935842f9db880ef25 # v5
        if: vars.CI_SKIP_SOURCE_PASSES_LINT != 'true' && vars.CI_SKIP_GO_VET != 'true'
        with:
          path: ${{ runner.temp }}/ci-cache/go
          key: ci-cache-go-${{ github.run_id }}
      - name: hadolint
        if: vars.CI_SKIP_SOURCE_PASSES_LINT != 'true' && vars.CI_SKIP_HADOLINT != 'true'
        uses: projectfile-org/actions/run-tool@78f06c9316d295ea7b646ad1bdac87b76b7f999a # v1
        with:
          image: ${{ vars.HADOLINT_IMAGE || 'hadolint/hadolint' }}
          version: v2.15.1
          pull: missing
          run: hadolint Dockerfile
      - name: ignorelint-check
        if: vars.CI_SKIP_SOURCE_PASSES_LINT != 'true' && vars.CI_SKIP_IGNORELINT_CHECK != 'true'
        uses: projectfile-org/actions/run-tool@78f06c9316d295ea7b646ad1bdac87b76b7f999a # v1
        with:
          image: ${{ vars.IGNORELINT_IMAGE || format('{0}/ignorelint', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: ignorelint
